# 独立部署版验收记录

验证日期：2026-09-22。使用独立临时 SQLite、虚构事件和本机模型 fixture，未读取或修改任何个人记忆数据。

| 项目 | 结果 |
| --- | --- |
| Python 完整回归 | 66/66 通过 |
| TypeScript 与生产构建 | 通过 |
| 浏览器实际操作 | Token 登录/失败、配置服务端保存、密钥不回传、片段入库、固定/取消固定、软归档、退出登录通过 |
| 布局 | 320 / 390 / 1280 px，无页面横向溢出 |
| 无障碍 | 四页浅色与深色通过 axe WCAG A/AA 自动检查 |
| Playwright | 5/5 通过，使用真实临时 HTTP 后端 |
| 工人 HTTP 集成 | 四种接口使用各自已保存的模型与 Key；提取、独立复核、情景帧真实执行并入库 |
| 原生情绪 | 八基础维度/关系隔离、显著变化、证据校验、过期边界、动态命名、并发去重、跨 turn 来源与唤醒限制通过 |
| Docker | 提供多阶段 Dockerfile 和 Compose；本机引擎未运行，未验证容器实际构建/启动 |

新增公开代码来自经授权只读查看的 v2 架构，已移除私人名称、私有环境监控路径和私人提示锚依赖。移植时保留原公开的通用提示模板。没有复制生产数据库、事件、密钥或运行配置。

原公开快照的两项失败已修复：审核不可用时不再返回未经审核的 fallback；确认注入后 30 分钟内不重复返回同版记忆。同时修复 tier 选择覆盖计时变量而导致召回延迟失真的问题。旧情绪衰减测试从 v1 的单一固定数值改为验证 v2 双核向基线衰减的行为；不会改动旧事件记录。

未验证：真实付费模型服务商的兼容性、公网域名/TLS、实际手机设备、Docker 引擎启动和任何生产部署。模型测试 fixture 通过不等于每家供应商都支持 JSON 输出、embedding 或 rerank；页面提供实际连接测试。

使用范围：单所有者、默认 namespace 界面。时间线和全库搜索支持分页，其他历史面板默认最近 100 条。没有聊天主模型、消息发送器或多租户授权系统。
